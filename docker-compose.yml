services:
  addigest:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./news.db:/app/news.db
      - ./chroma_db:/app/chroma_db
    env_file:
      - .env
    restart: unless-stopped
